name: Ping AM Muse bot

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Call health check
        run: curl --retry 3 --retry-delay 5 --max-time 30 \
          -f https://liquid245-project-am-muse.hf.space/
